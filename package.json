{
  "name": "readable",
  "engines": {
    "node": "14.x.x"
  },
  "scripts": {
    "build": "concurrently \"cd backend && npm install\" \"cd frontend && yarn install && yarn build\"",
    "deploy": "mkdir public && cp -a frontend/build/. public/",
    "heroku-postbuild": "npm run build && npm run deploy"
  },
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
