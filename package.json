{
  "name": "readable",
  "engines": {
    "node": "8.10.0"
  },
  "dependencies": {
    "concurrently": "^3.1.0",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "build": "concurrently \"cd frontend && yarn build\" \"cd api-server && yarn build\"",
    "clean": "concurrently \"rimraf node_modules\" \"cd frontend && rimraf node_modules build\" \"cd api-server && rimraf node_modules build\"",
    "heroku-postbuild": "yarn build",
    "install": "(cd frontend && yarn) && (cd api-server && yarn)",
    "start": "concurrently \"cd frontend && PORT=3000 yarn start\" \"cd api-server && node server\"",
    "start:prod": "cd api-server && node server"
  }
}