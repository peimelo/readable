{
  "name": "readable",
  "engines": {
    "node": "8.10.0"
  },
  "scripts": {
    "build": "concurrently \"cd api-server && npm install\" \"cd frontend && npm install && npm run build\"",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
