{
  "name": "readable",
  "engines": {
    "node": "8.10.0"
  },
  "scripts": {
    "build": "cd api-server && npm install && cd .. && cd frontend && npm install && npm run build && cd ..",
    "deploy": "mkdir public && cp -a frontend/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}