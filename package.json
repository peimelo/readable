{
  "name": "readable",
  "engines": {
    "node": "8.10.0"
  },
  "scripts": {
    "build": "concurrently \"cd api-server && npm install\" \"cd frontend && npm run build\"",
    "deploy": "mkdir public && cp -a frontend/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  },
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
