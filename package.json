{
  "name": "readable",
  "engines": {
    "node": "14.x.x"
  },
  "scripts": {
    "build": "concurrently \"cd api && npm install\" \"cd frontend && npm install && npm run build\"",
    "deploy": "mkdir api/public && cp -a frontend/build/. api/public/",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
